-- funcs.py and funcsAux.py are python files that are included in other functions to obtain the wavelet scalograms. They are not run by themselves
-- The .pkl input to the programs depend on where the result of netPyNE simulations are saved. That needs to be checked before running these.
-- Hist_AutoCorr.py plots spike-time hostograms for the SCs and the PV neurons based on simulations using the .pkl files. Using single explicit filename would be better. For automation, the code needs to be edited. This code also generates auto-correlation plots using the histograms as the input.
-- GE_Analysis_Batch.py generates .csv or .txt files as outputs for 15 different seed formulations and simulations varying E->I conductance. Fig6_Summary_Plots.py generate the box plots in the bottom most panels of figure 6.
-- GEGI_Analysis_Batch.py generates .csv or .txt files as outputs for simulations varying E->I conductance and I->E conductance simultaneously. But we ran 9 such simulations ofr 9 seeds. Current batch.py has possibility of doing them all parallely and thus the file name converter needs to be modified. Fig7_Heat_Plots.py plots the heat plots in the top panel of Figure 7. There are multiple color grade formats that this presents as output.
-- The .txt and .xlsx files are data provided for the summary plots, which were compiled and converted from the outputs in the form of .csv or .txt to get the plots. These can be generated and automated to plot directly as well.
-- Single_Sims_Analysis.py generates individual figures for simulations, with IPSCs and scalograms as in figure 6. The files names need to be changed to obtain the exact figure. 
-- The power_frequency data was converted to .xlsx to use pandas and seaborn to obtain the box plots. Box_Plot_All.py and Box_Plot_Freq.py can be used to plot box plots as in Figure 6. The x-axis values are set to the weights used in pS. These needs to be multiplied by 120 to get the values in nS (nano Seimens). The latter code has the split of frequency values at the 100pS weight or 12nS conductance. 
